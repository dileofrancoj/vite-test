import { Col } from "react-bootstrap";

const Favorites = (props: unknown) => {
    return (
        <>
            <Col xs={4} lg={4} style={{ backgroundColor: "lightgray", height: "100vh" }}>
                {/* Tu contenido aquí */}
            </Col>
        </>
    );
};

export default Favorites;
